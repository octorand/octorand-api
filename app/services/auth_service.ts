export default class AuthService {
  /**
   * Setup authentication parameters
   *
   * @returns
   */
  setup() {
    return {};
  }

  /**
   * Verify authentication parameters
   *
   * @param payload
   * @returns
   */
  verify(payload: any) {
    return payload;
  }
}
